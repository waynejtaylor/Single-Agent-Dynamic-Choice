The files are meant to be used in the following order:

1) Use "Rust Data Generating Process" to derive the "bus_df_lin.csv" file

2) Use "Import Data and Estimate" to estimate the parameters, which calls on "npl_sing.R" and "clogit.R"